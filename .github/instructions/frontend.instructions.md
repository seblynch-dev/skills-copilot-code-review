---
applyTo: "src/static/**/*.html,src/static/**/*.css,src/static/**/*.js"
---

## Frontend Guidelines

- Use accessibility attributes (e.g., `alt` text and `aria-label`/`aria-labelledby`) and ensure sufficient color contrast.
- Use responsive design for compatibility with mobile devices.
- Validate HTML structure and semantic elements.